# === N8N ===
N8N_BASIC_AUTH_USER=safira
N8N_BASIC_AUTH_PASSWORD=senha_supersegura
N8N_PORT=5678

# === Venom / WhatsApp ===
VENOM_API_KEY=sua_chave_api

# === LLM Ollama ===
MODEL_NAME=llama3

# === SESANE ===
SESAME_CONFIG=/app/config/default.yaml

# === Whisper ===
WHISPER_MODEL=medium

# === Coqui ===
COQUI_MODEL=tts_model_id

# === Prometheus ===
PROMETHEUS_CONFIG=/etc/prometheus/prometheus.yml

# === Grafana ===
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin123
GRAFANA_PORT=3001

# === Traefik ===
TRAEFIK_ENTRYPOINTS=web
TRAEFIK_API=true

# === NGINX ===
NGINX_PORT=8081

# === Redis ===
REDIS_PASSWORD=senha_redis_segura

# === MinIO ===
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# === Jira ===
JIRA_DB=jiradb
JIRA_LICENSE_KEY=sua_licenca_jira

# === Jenkins ===
JENKINS_ADMIN_ID=admin
JENKINS_ADMIN_PASSWORD=admin123
JENKINS_URL=http://localhost:8083

# === BLIP2 ===
BLIP2_MODEL=blip2_vit

# === Stable Diffusion ===
SD_MODEL=sd_1-5
SD_API_KEY=sua_chave_sd

# === PostgreSQL ===
POSTGRES_USER=postgres
POSTGRES_PASSWORD=senha_pg_segura
POSTGRES_DB=safira